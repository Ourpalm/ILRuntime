<section class="sidebar clearfix">
    <ul>
        <% site.pages.find({type: page.type , lang: page.lang}).sort('order').each(function (p) { %>
            <% var fileName = p.path.replace(/^.+?\/([\w-]+)\.html/, '$1') %>
            <% if(fileName == 'index'){%>
                <li><h3>基础</h3></li>
            <%}%>
            <% if(fileName == 'tutorial'){%>
                <li><h3>教程</h3></li>
            <%}%>
            <li>
                <p><a href="<%- url_for("/ILRuntime/" + p.path) %>" class="sidebar-link<%- page.title === p.title ? ' current' : '' %><%- p.is_new ? ' new' : '' %>"><%- p.title %></a></p>
            </li>
        <% }) %>
    </ul>
</section>